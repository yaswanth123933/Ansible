- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print name
    ansible.builtin.debug:
      msg: "Hello {{ item }}" #here item is reserved keyword
    loop:
    - ramesh
    - raju
    - yash

 