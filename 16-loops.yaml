- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: print name
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip

 