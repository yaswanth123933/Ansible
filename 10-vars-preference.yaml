- name: vars preference
  hosts: local
  connection: local
  vars:
    NAME: from play level
  # vars_files:
  # - course.yaml
  vars_prompt:
  - name: NAME
    prompt: what is your NAME?
    private: true 
  tasks:
  - name: testing variables preference
    # vars:
    #   NAME: from play level
    ansible.builtin.debug:
      msg: "Name: {{ NAME }}"

      #1st preference is from arguments or command lines
      #2nd preferenc e is from promt
      #3rd preference is for files
      

